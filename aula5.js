const leia = require("prompt-sync")({sigint:true})
var nome;
var nota1
nome =leia ("Informe seu nome: ");
note1 = leia("Informe sua nota")
if (note1 >=7)
(
    console.log("você está aprovado")

)
else
(
    console.log ("Reprovado ")
)